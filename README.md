## Usage
1. Copy your `app.asar` into this folder
2. Install dependencies: `npm install --save`
3. Run `npm run unpack` to throw the JS into the 'data' folder
4. Edit the JS
5. Run `npm run pack`
6. Copy the `app.asar` file back to wherever you found it
